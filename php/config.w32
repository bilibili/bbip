// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("sinaip", "for sinaip support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("sinaip", "enable sinaip support", "no");

if (PHP_SINAIP != "no") {
	EXTENSION("sinaip", "sinaip.c");
}

